#!/bin/sh

BUILD_DIR=$1
CONFIG_FILE_PATH=config/config.alloy

if [ -f "$BUILD_DIR/$CONFIG_FILE_PATH" ]; then
  echo "Alloy config detected."
  exit 0
else
  echo "Missing Alloy config ($CONFIG_FILE_PATH)."
  exit 1
fi
